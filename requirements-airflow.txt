# Apache Airflow and related packages
# Install AFTER requirements-core.txt is working
# Note: Airflow installation on Windows can be complex

# Apache Airflow
apache-airflow==2.7.3
apache-airflow-providers-google==10.10.1

# Additional constraints for Airflow on Windows
# See: https://airflow.apache.org/docs/apache-airflow/stable/installation/
